{
  "jest.debugCodeLens.showWhenTestStateIn": ["skip", "pass", "fail", "unknown"],
  "cSpell.words": [
    
  ],

  "files.exclude": {
    // Hide all files that end with .js if they have a .ts file
    "**/*.js": {
      "when": "$(basename).ts"
    },
    "**/*.d.ts": true,
  }
}
